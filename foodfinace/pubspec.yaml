name: bill_splitting_app
description: A bill splitting app for food expenses.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
  supabase_flutter: ^1.10.25
  mysql1: ^0.20.0
  postgres: ^2.6.3
  sdk: flutter
  sqflite: ^2.3.0
  provider: ^6.0.5
  image_picker: ^1.0.5
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
